services:
  - type: web
    name: otc-analyzer
    runtime: python
    buildCommand: ""
    startCommand: "uvicorn main.main:app --host 0.0.0.0 --port 10000"
    envVars:
      - key: PYTHON_VERSION
        value: 3.11
